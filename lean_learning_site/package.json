{
  "name": "lean-learning-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/sync-books.mjs",
    "preview": "npm run build && python3 -m http.server 8787",
    "deploy": "npm run build && npx wrangler pages deploy . --project-name lean-learning-site"
  }
}